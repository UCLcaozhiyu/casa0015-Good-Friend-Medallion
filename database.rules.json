{
  "rules": {
    "users": {
      "$uid": {
        "location": {
          ".read": "auth != null || true",  // 测试模式下允许所有读取
          ".write": "auth != null || true"  // 测试模式下允许所有写入
        }
      }
    }
  }
} 